# ============================================================
#  火灾烟雾智能识别系统 —— 开发 / CI 依赖
# ============================================================
#  用法（在 backend 目录下）：
#      pip install -r requirements-dev.txt
#
#  说明：本文件只在开发、自检与 CI 中使用，**部署后端服务不需要**。
#        httpx 仅被 scripts/selftest.py 用于发 HTTP 请求，app/ 下不 import 它，
#        因此单独放在这里，避免给生产环境增加无用依赖。
# ============================================================

-r requirements.txt

# --- 端到端自检脚本（scripts/selftest.py）---
httpx>=0.27
